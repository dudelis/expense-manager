@model IEnumerable<AccountTypeViewModel>
@{
    ViewData["Title"] = "Account Types";
    AccountTypeViewModel firstType = Model.ToList().FirstOrDefault();
}

<h4>@ViewData["Title"]</h4>

<p>
    <a asp-action="Create" class="btn btn-primary btn-sm">New Account Type</a>
</p>
<div class="table-responsive-sm">
    <table class="table">
        <thead>
            <tr>
                <th>
                    <label asp-for="@firstType.Name"></label>
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        <label>@item.Name</label>
                    </td>
                    <td>
                        <div class="btn-toolbar float-right">
                            <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-sm"><i class="material-icons">edit</i></a>
                            <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-sm"><i class="material-icons">delete</i></a>
                        </div>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
