@page "/settings"

@inject HttpClient httpClient;
@inject NavigationManager navigationManager;

<BSContainer IsFluid="true">
    <BSRow>
        <BSCol>
            <AccountTypeList />
        </BSCol>
    </BSRow>
    <BSRow>
        <BSCol>
            <CurrencyList />
        </BSCol>
    </BSRow>
</BSContainer>

@code {

}
